<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_helpdesk_ticket_tree" model="ir.ui.view">
        <field name="name">view.helpdesk_ticket.tree</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <tree string="Ticket">
               <field name="name"/>
               <field name="date"/>
               <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_helpdesk_ticket_form" model="ir.ui.view">
        <field name="name">view.helpdesk_ticket.form</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <form string="Ticket">
                <header>
                <!--Asignar, solo visible con estado nuevo -->
                <button string="Asignar" name="asignar" type="object" states="nuevo"/>
                <!--Proceso, solo visible con estado asignado -->
                <button string="En proceso" name="proceso" type="object" states="asignado"/>
                <!--Finalizar, visible con todos los estados -->
                <button string="Finalizar" name="resuelto" type="object" states="nuevo,asignado,proceso,pendiente"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                <div class="oe_title">
                    <h1>
                        <field name="name"/>
                    </h1>
                </div>
                <group>
                    <group>
                        <field name="date"/>
                        <field name="date_limit"/>
                    </group>
                    <group>
                        <field name="user_id"/>
                        <field name="assigned"/>
                        <field name="ticket_qty"/>
                        <field name="time"/>
                    </group>
                </group>
                <notebook>
                    <page string="Description">
                        <field name="description"/>
                        <group>
                            <field name='tag_name'/>
                            <button string="Create tag" name="create_tag" type="object"/>
                        </group>
                        <field name="tag_ids"/>
                    </page>
                    <page string="Actions">
                        <field name="action_ids"/>
                    </page>
                    <page string="Quality">
                        <group string="Corrective Action">
                            <field name="action_corrective" nolabel="1"/>
                        </group>
                        <group string="Preventive Action">
                            <field name="action_preventive" nolabel="1"/>
                        </group>
                    </page>
                </notebook>
               </sheet>
            </form>
        </field>
    </record>


</odoo>